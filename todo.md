# To Do

- Build in a temp directory (`/tmp/slug/medium/...`)
- Tell each rake file where to put its final output.
- Build mobi file from coverless epub.
- Combine `ebook/format` and `ebook/template` into one git project.
- Combine `paperback/format` and `paperback/template` into one git project.
- Build paperback format in a dir of its own (not under paperback files).
- Pass dbp.fmt path to paperback rake file.
- Epub details.
- Product description in metadata file.
- Improve `ebook/template/Rakefile`
- Use common ebook content where possible
